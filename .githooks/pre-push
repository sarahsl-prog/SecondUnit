#!/bin/bash
# .githooks/pre-push — blocks `git push` if tests or lint fail.
# Enable once per clone: git config core.hooksPath .githooks
set -e

echo "[pre-push] running pytest..."
uv run pytest -q

echo "[pre-push] running ruff..."
uv run ruff check .

echo "[pre-push] OK"
